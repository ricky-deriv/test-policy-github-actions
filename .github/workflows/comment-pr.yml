name: My Workflow
on:
  pull_request:
    types: [opened, synchronize]
jobs:
  initialize:
    name: "get dirs"
    runs-on: ubuntu-latest
    outputs:
      matrix: ${{ steps.set-matrix.outputs.matrix }}
    steps:
      - id: set-matrix
        run: |
          echo matrix="$(jq -Mc -n '{"dir":["aws","project"]}')" >>$GITHUB_OUTPUT
  terraform: 
    needs: initialize
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{fromJson(needs.initialize.outputs.matrix)}}
    outputs:
      plan-json: ${{ steps.opa-result.outputs.opa-result }}
    steps:
      - uses: actions/checkout@v3
      - name: archive terraform json plan
        uses: actions/upload-artifact@v3
        with:
          name: tf-plan
          path: aws/tfplan.json
          retention-days: 5
      - name: test-matrix
        run: |
          echo "testing matrix"
          echo "dir: ${{ matrix.dir }}"
      - name: print result into github output
        if: always()
        id: opa-result
        working-directory: ${{ matrix.dir }}
        run: |
          ls
          output="$(jq -c . tfplan.json)"
          echo "opa-result=${output}" >> "${GITHUB_OUTPUT}"
  opa-eval:
    needs: [ terraform, initialize ]
    strategy:
      matrix: ${{fromJson(needs.initialize.outputs.matrix)}}
    uses: ./.github/workflows/opa.yml
    with: 
      tfplan: ${{ needs.terraform.outputs.plan-json }}
      dir: ${{ matrix.dir }}
  display-output:
    needs: [ initialize, opa-eval ]
    runs-on: ubuntu-latest
    strategy:
      matrix: ${{fromJson(needs.initialize.outputs.matrix)}}
    steps:
      - uses: actions/checkout@v3  
      - name: name artifacts
        working-directory: ${{ matrix.dir }}
        run: |
          new_name=$(echo ${{ matrix.dir }} | tr '/' ' ')
          echo "artifact-name=${new_name}" >> "${GITHUB_ENV}"
      - name: get opa-results
        uses: actions/download-artifact@v3
        with:
          name: ${{ env.artifact-name }}-opa-result
      - name: print result into github output
        if: always()
        id: opa-result
        run: |
          pwd
          echo "next ls current.."
          ls
          # output="$(jq -c . opa-result.json)"
          # echo "result=${output}" >> "${GITHUB_OUTPUT}"
          # cat opa-result.json
          
          EOF=$(dd if=/dev/urandom bs=21 count=1 status=none | base64)
          echo "result<<$EOF" >>"$GITHUB_OUTPUT"
          cat  >>"$GITHUB_OUTPUT" opa-result.json
          echo "$EOF" >>"$GITHUB_OUTPUT"
      - uses: actions/github-script@v6
        if: always()
        with:
          script: |
            let output = `### open policy agent check: \`test\`
            Directory: \`${{ matrix.dir }}\`
            <details><summary>policy check output</summary>
            
            \`\`\`diff\n
            ${{ steps.opa-result.outputs.result }}
            \`\`\`
            </details>
            
            end`
            
            output = output.replace('"', '\"')
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })
